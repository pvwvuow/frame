# سیاست نسخه‌گذاری فریم (Versioning Policy)

فریم از **نسخه‌گذاری معنایی (Semantic Versioning)** استفاده می‌کند؛ عدد هر نسخه واقعاً مفهوم دارد و از روی محتوای تغییرات تعیین می‌شود، نه صرفاً «یک عدد جلوتر».

## قالب: `MAJOR.MINOR.PATCH` — مثال: `0.11.0`

| بخش | کی بالا می‌رود | مثال واقعی |
|------|----------------|------------|
| **MAJOR** (x.0.0) | تغییر بزرگ و ناسازگار — مهاجرت داده‌ای اجباری، تغییر حساب‌ها/فرمت پایگاه‌داده به شکلی که نسخه قبلی نتواند کار کند، بازطراحی بنیادی محصول | انتشار 1.0 عمومی (آینده) |
| **MINOR** (0.x.0) | **قابلیت یا تجربه‌ی جدید محسوس** — بخش/صفحه جدید، بازطراحی یک جریان کاربری، امکانات تازه (کالکشن‌ها، تاریک‌خانه، سینک ابری)، دور گردِ بزرگ رفع اشکال اندروید | `0.11.0` بازطراحی موبایل‌محور اندروید |
| **PATCH** (0.0.x) | **رفع اشکال بدون قابلیت جدید** — کرش، صفحه‌ی خالی، نشتی داده، خرابی پخش؛ به‌علاوه بهبودهای ریزی که چیزی به محصول اضافه نمی‌کنند | `0.10.33` رفع «لیست من» باز نمی‌شد |

### قوانین عملی

1. **هر تغییر کد = تصمیم نسخه.** قبل از کامیت، طبق جدول بالا مشخص می‌کنیم این تغییر PATCH است یا MINOR (و به‌ندرت MAJOR).
2. **صفرِ ابتدایی (0.x)** یعنی محصول در حال تکمیل است و ممکن است API/داده بین نسخه‌های minor تغییر کند. رسیدن به **1.0.0** = تثبیت سازگاری.
3. **versionCode اندروید** خودکار از نسخه محاسبه می‌شود: `major×1,000,000 + minor×10,000 + patch` (مثلاً `0.11.0` → `110000`) و همیشه صعودی است.
4. **به‌روزرسانی خودکار دسکتاپ** از `latest.yml` استفاده می‌کند و مبتنی بر مقایسه‌ی semver است — رعایت همین قاعده شرط کارکرد آن است.
5. پیام کامیت با نسخه شروع می‌شود: `v0.11.0: <خلاصه تغییرات>`.
6. برای هر نسخه در گیت‌هاب **Release Notes** واقعی می‌نویسیم (لیست قابلیت‌ها/رفع اشکال)، چون کاربر باید بداند این نسخه چه چیززی برایش دارد.

### تاریخچه — نسخه‌های واقعی منتشرشده

| نسخه | نوع | محتوا |
|------|------|--------|
| `0.10.29` | minor-اندروید | اولین نسخه اندروید (Capacitor) + APK در Release مشترک |
| `0.10.30` | patch+ | رفع ناوبری/آیکون/آپدیتر اندروید |
| `0.10.31` | patch | رفع کرش راه‌اندازی دسکتاپ بعد از ادغام اندروید |
| `0.10.32` | minor* | کالکشن‌های کاربری + سینک ابری واقعی (می‌بایست 0.11.0 می‌شد؛ از این‌جا به بعد رعایت می‌کنیم) |
| `0.10.33` | patch | مهاجرت خودکار db قدیمی؛ رفع «لیست من» |
| `0.10.34` | minor* | دیوار کالکشن‌های کاربران در تاریک‌خانه + قالب‌های آماده |
| `0.10.35` | patch* (امنیتی) | جداسازی فضای داده‌ی هر حساب کاربری |
| `0.10.36` | minor* | کارت‌های عکس کالکشن در تاریک‌خانه (۶ قالب) + رفع هاله لوگوی VIP |
| `0.11.0` | **minor** | **بازطراحی موبایل‌محور اندروید**: لینک‌های پویا، دکمه‌های لمسی کارت، دکمه‌ی بازگشت سخت‌افزاری، safe-area کامل، صفحه‌ی هنرمندان سبک‌شده + سیستم نسخه‌گذاری و README حرفه‌ای |

\* نسخه‌های قبل از `0.11.0` فقط شماره‌ی ترتیبی بودند؛ از `0.11.0` به بعد عدد نسخه دقیقاً از روی نوع تغییر محاسبه می‌شود.
