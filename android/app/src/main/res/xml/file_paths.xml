<?xml version="1.0" encoding="utf-8"?>
<!-- v0.17.0 — scoped to the app's private files dir (offline downloads,
     OTA bundles, cover packs, exported subtitle files). The unused
     external-path/cache-path grants were removed (least privilege). -->
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <files-path name="files" path="." />
</paths>
