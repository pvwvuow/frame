<div dir="rtl">

# ✅ وضعیت پیاده‌سازی — نسخه v0.27.0

این فایل سند «بازبینی جامع فریم» است (نسخه ۰.۲۶.۰). **تمام ۳۲ مورد فاز ۱ تا ۳ در v0.27.0 پیاده شد** — جزئیات هر فیکس داخل کد با برچسب `[DATA-n] / [UI-n] / [A11Y-n] / [QOL-n]` ثبت شده است:

| فاز | موارد | وضعیت |
|---|---|---|
| فاز ۱ — بحرانی | DATA-1..4 · UI-1..3 | ✅ همگی فیکس شد |
| فاز ۲ — مهم | DATA-5..16 · UI-4..7 | ✅ همگی فیکس شد |
| فاز ۳ — پولیش | A11Y-1..5 · QOL-1..4 | ✅ همگی فیکس شد |
| فیچرها (پیشنهاد) | FEAT-1..15 | 🗺️ نقشه راه نسخه‌های بعد (جستجوهای اخیرِ UI-4 عملاً بخشی از FEAT-9 را پوشش می‌دهد) |

**معماری جدید سینک (خلاصه):** صف عملیات آفلاین + سنگ‌قبر محلی (`src/lib/sync-queue.ts`)، انتشار حذف‌ها بین دستگاه‌ها با رویداد `sync_del` روی `user_events` (بدون تغییر اسکیمای Supabase)، LWW واقعی برای لیست/امتیاز، پیشرفت per-episode (دسکتاپ: جدول `WatchEpisodeProgress`؛ موبایل: ردیف per-episode در Dexie)، merge فیلدبه‌فیلد پروفایل با مهر زمانی per-scope، و سینک پربرفرنس‌های پلیر روی `profiles.data.playerPrefs`.

---

<div dir="rtl">

# 🎬 بازبینی جامع فریم — تجربه کاربری، ذخیره داده و قابلیت‌ها

> **دامنه:** رابط کاربری (UI/UX)، پایداری و سینک داده کاربر، و شکاف‌های محصولی
> **روش:** بررسی مستقیم کد ریپو `pvwvuow/frame` (نسخه ۰.۲۶.۰) — سه بررسی موازی روی صفحات، کامپوننت‌ها و لایه‌های ذخیره‌سازی
> **زاویه دید:** این گزارش **کاربر-محور** است، نه دیدِ توسعه‌دهنده. یعنی چیزهایی که کاربر واقعی هنگام کار با اپ می‌بیند، لمس می‌کند، گیج می‌شود، یا داده‌اش را از دست می‌دهد.

## چطور این گزارش را بخوانید

فایل `AUDIT.md` موجود در ریپو یک ممیزی **فنی/امنیتی** است (SSRF، CI، heap و…). این گزارش آن را تکرار نمی‌کند؛ مکملِ آن است و روی سه چیزی تمرکز دارد که شما خواستید:

1. **رابط کاربری** — چیزی که کاربر می‌بیند و اذیت می‌شود
2. **ذخیره داده** — چیزی که باید درست ذخیره/سینک شود و نمی‌شود
3. **قابلیت‌های گمشده** — چیزی که بهتر است باشد و نیست

هر بخش **فاز‌به‌فاز** اولویت‌بندی شده:

- **فاز ۱ — بحرانی:** از دست رفتن داده، عملیات غیرقابل‌دسترس، یا سردرگمی جدی کاربر. همین حالا.
- **فاز ۲ — مهم:** تجربه ناقص، شکاف قابلیت مهم، ناسازگاری سینک.
- **فاز ۳ — پولیش:** دسترس‌پذیری، QoL، و بهبودهای تدریجی.

هر یافته: `[شناسه]` عنوان · مکان در کد · مشکل از دید کاربر · راه‌حل.

---

# 🔴 فاز ۱ — بحرانی (از دست رفتن داده و مسدودیت)

این‌ها مواردی هستند که یا **داده کاربر را از بین می‌برند**، یا **باعث می‌شوند کاربر فکر کند اپ خراب است**. باید اول این‌ها حل شوند.

## بخش ذخیره داده — «رستاخیز داده» و حذفی که کار نمی‌کند

بزرگ‌ترین مشکل ساختاری اپ اینجاست: **مدل سینک فقط «اضافه می‌کند، هرگز حذف نمی‌کند» (union-only).** این یعنی وقتی چیزی را حذف می‌کنید، ممکن است دوباره برگردد.

### `[DATA-1]` حذف علاقه‌مندی/امتیاز/لیست در حالت آفلاین گم می‌شود و دوباره زنده می‌شود
- **کجا:** `src/components/library/LibraryProvider.tsx:164,216` + `src/lib/cloud.ts:649,675`
- **مشکل کاربر:** وقتی اینترنت (یا VPN) وصل نیست و شما فیلمی را از علاقه‌مندی‌ها برمی‌دارید، این حذف فقط به‌صورت «push فوری» تلاش می‌شود و در حالت آفلاین بی‌سروصدا شکست می‌خورد. برخلاف حذف کالکشن، برای علاقه‌مندی/امتیاز/لیست **هیچ صف حذف آفلاینی وجود ندارد.** دفعه بعد که آنلاین می‌شوید، سینک همان علاقه‌مندی را از ابر «پر می‌کند» و قلب دوباره روشن می‌شود. کاربر: «چرا فیلمی که پاک کردم دوباره برگشت؟»
- **راه‌حل:** یک جدول «سنگ قبر» (tombstone) محلی برای حذف‌ها (slug + زمان) که هنگام آنلاین‌شدن قبل از pull اجرا شود؛ یا سینک مبتنی بر `deletedAt`.

### `[DATA-2]` استراتژی بنیادی سینک «فقط union» است — حذف هرگز بین دستگاه‌ها منتقل نمی‌شود
- **کجا:** `src/app/api/cloud/merge/route.ts:19-20` («nothing is ever deleted locally by a sync»)
- **مشکل کاربر:** طراحی سینک این است: «ابر فقط جای خالی را پر می‌کند، محلی برنده است، هیچ‌وقت حذف نمی‌کند». نتیجه: حذف در دستگاه A فقط وقتی به دستگاه B می‌رسد که push فوری آنلاین موفق شود. اگر B آن لحظه آفلاین بود، آیتم حذف‌شده روی B می‌ماند و در سینک بعدیِ B **دوباره به ابر push می‌شود** → چرخه بی‌پایان رستاخیز بین دو دستگاه. علاقه‌مندی، لیست تماشا و امتیاز همه آسیب‌پذیرند.
- **راه‌حل:** مدل سینک مبتنی بر tombstone با `updatedAt` سراسری؛ حذف = ردیفی با `deletedAt` که روی همه دستگاه‌ها اعمال شود.

### `[DATA-3]` حذف تاریخچه/ادامه تماشا اصلاً به ابر نمی‌رود
- **کجا:** `src/lib/mobile/userdata.ts:448-467` (`removeProgress`) — هیچ `pushProgressDelete` در `cloud.ts` وجود ندارد
- **مشکل کاربر:** وقتی فیلمی را از ردیف «ادامه تماشا» پاک می‌کنید (چون تمامش کرده‌اید یا خصوصی است)، فقط ردیف محلی پاک می‌شود. هیچ تابعی موقعیت را از جدول ابری `watch_progress` حذف نمی‌کند. چون سیاست «پر کردن جای خالی» است، در سینک بعدی همان فیلم دوباره در «ادامه تماشا» ظاهر می‌شود.
- **راه‌حل:** افزودن `pushProgressDelete(slug)` که در `removeProgress` صدا زده شود + صف tombstone.

### `[DATA-4]` هر عملیات آفلاین «انگار ذخیره شد» نشان می‌دهد ولی هرگز سینک نمی‌شود
- **کجا:** `src/lib/cloud.ts:514-516,591,684-695` + toast موفقیت در `LibraryProvider.tsx:166`
- **مشکل کاربر:** هر push اول باید `id → slug` را از `/api/title/cloud-key` بگیرد؛ آفلاین این fetch شکست می‌خورد و تابع بی‌صدا برمی‌گردد. اما UI **قبلاً** توست «به علاقه‌مندی‌ها اضافه شد» را نشان داده. کاربر مطمئن است سینک شده — ولی نشده و هیچ صف retry هم وجود ندارد.
- **راه‌حل:** صف عملیات معلق + عدم ادعای «سینک شد» تا تأیید واقعی.

## بخش رابط کاربری — عملیات غیرقابل‌دسترس و خطای پنهان

### `[UI-1]` دکمه‌های حذف/سنجاق/یادداشت در نمای شبکه‌ای روی موبایل کاملاً نامرئی‌اند
- **کجا:** `src/components/library/MyListManager.tsx:543-544`
- **مشکل کاربر:** دکمه‌های اکشن هر پوستر با `opacity-0 group-hover:opacity-100` مخفی‌اند — یعنی فقط با نگه‌داشتن ماوس دیده می‌شوند. روی گوشی و تبلت که hover وجود ندارد، کاربر **اصلاً نمی‌تواند** در نمای شبکه‌ای آیتمی را حذف، سنجاق یا یادداشت‌گذاری کند. این قابلیت عملاً روی موبایل وجود ندارد.
- **راه‌حل:** روی صفحات لمسی دکمه‌ها همیشه نمایش داده شوند (media hover query) یا یک منوی «⋯» همیشه‌قابل‌لمس اضافه شود.

### `[UI-2]` خطای شبکه در جستجو بی‌صدا خورده می‌شود و «چیزی پیدا نشد» نشان می‌دهد
- **کجا:** `src/components/Navbar.tsx:154-155,355`
- **مشکل کاربر:** `.catch(() => {})` هر خطای شبکه را می‌بلعد؛ بعد چون نتایج خالی است، پیام «برای X چیزی پیدا نشد» ظاهر می‌شود. کاربرِ قطع‌شده فکر می‌کند آن فیلم وجود ندارد، نه اینکه اتصالش مشکل دارد. (همین الگو در `CommandPalette.tsx:88-89` هم هست.)
- **راه‌حل:** نگه‌داشتن state خطا و نمایش «خطا در جستجو — دوباره تلاش کنید» با دکمه تلاش مجدد.

### `[UI-3]` لغو دانلودِ در حال انجام بدون تأیید و بدون بازگردانی
- **کجا:** `src/components/download/DownloadsClient.tsx:234-237`
- **مشکل کاربر:** دکمه لغو (X) بلافاصله دانلود در حال پیشرفت را کنسل می‌کند؛ بدون هیچ تأیید و بدون undo. یک لمس اشتباه، ساعت‌ها دانلود را نابود می‌کند. این با استاندارد خودِ اپ (که برای حذف لیست، تأیید و undo دارد) ناهماهنگ است.
- **راه‌حل:** تأیید برای لغو دانلود در حال انجام، یا Toast با دکمه «واگرد».

---

# 🟠 فاز ۲ — مهم (تجربه ناقص و ناسازگاری سینک)

این‌ها اپ را نمی‌شکنند، اما تجربه را ناقص و گاهی گیج‌کننده می‌کنند. مخصوصاً بخش سینک اینجا مشکلات جدی دارد.

## بخش ذخیره داده — تعارض سینک که هرگز همگرا نمی‌شود

### `[DATA-5]` امتیاز و وضعیت لیست بین دو دستگاه هرگز همگرا نمی‌شود
- **کجا:** `src/app/api/cloud/merge/route.ts:123,126-142` + `src/lib/mobile/userdata.ts:1020-1042`
- **مشکل کاربر:** سیاست merge برای watchlist و ratings فقط ردیف‌های **نبوده** را اضافه می‌کند و هرگز مقدار موجود محلی را با ابر بازنویسی نمی‌کند. جدول‌های ابری ستون `updated_at` دارند اما merge از آن استفاده نمی‌کند. سناریو: روی موبایل به فیلمی امتیاز ۷ می‌دهید، بعد روی دسکتاپ همان را ۹ می‌کنید. موبایل **تا ابد** ۷ نشان می‌دهد چون ردیف محلی «موجود» است. کاربر: «چرا امتیازم روی گوشی سینک نشد؟»
- **راه‌حل:** LWW واقعی بر اساس `updated_at` برای هم `watchlist.status` و هم `ratings.score` (همان کاری که برای progress انجام شده).

### `[DATA-6]` یادداشت، سنجاق و تاریخ برنامه‌ریزی لیست تماشا اصلاً سینک نمی‌شوند
- **کجا:** `src/lib/cloud.ts:661` (فقط status) در برابر `schema.prisma:81-84` (note/pinned/plannedDate)
- **مشکل کاربر:** ابر فقط `status` را ذخیره می‌کند. یادداشتی که روی یک فیلم می‌نویسید، سنجاق (pin)، و «تاریخ برنامه‌ریزی تماشا» (تقویم لیست من) هیچ‌وقت روی دستگاه دوم ظاهر نمی‌شوند و با خروج/ورود مجدد از بین می‌روند.
- **راه‌حل:** افزودن ستون‌های note/pinned/plannedDate به جدول ابری.

### `[DATA-7]` «ادامه تماشا» فقط یک اپیزود در هر سریال را نگه می‌دارد
- **کجا:** `prisma/schema.prisma:154` (`@@unique([userKey, titleId])`) + `src/lib/mobile/db.ts:102-104`
- **مشکل کاربر:** پیشرفت تماشا فقط **یک ردیف به‌ازای هر عنوان** است، نه به‌ازای هر اپیزود. سناریو: سریالی را تا فصل ۱ قسمت ۳ (دقیقه ۲۰) دیده‌اید، بعد فصل ۲ قسمت ۱ را شروع می‌کنید؛ حالا اگر بخواهید به قسمت ۳ برگردید، از **اول** شروع می‌شود چون موقعیت قبلی بازنویسی شده.
- **راه‌حل:** کلید یکتا را به `(userKey, titleId, episodeId)` تغییر بده و «ادامه تماشا» را آخرین اپیزود فعال نمایش بده.

### `[DATA-8]` تعارض پیشرفت بر اساس ساعت دستگاه حل می‌شود — جای پخش را عقب می‌برد
- **کجا:** `src/app/api/cloud/merge/route.ts:210` + `src/lib/cloud.ts:601,619`
- **مشکل کاربر:** «برنده‌شدن جدیدتر» با timestamp تولیدشده روی خود دستگاه سنجیده می‌شود. اگر ساعت گوشی اشتباه باشد (شایع روی اندروید بدون NTP)، پیشرفت واقعیِ جدیدترِ یک دستگاه با موقعیت قدیمی دستگاه دیگر بازنویسی می‌شود → پرش به عقب در «ادامه تماشا».
- **راه‌حل:** استفاده از ساعت سرور Supabase برای `updated_at`، یا استفاده از `max(position)` به‌عنوان tie-breaker.

### `[DATA-9]` سینک پروفایل «کل‌شیء» است، نه فیلد‌به‌فیلد — تنظیمات همدیگر را می‌خورند
- **کجا:** `src/lib/cloud.ts:392-394,406-417`
- **مشکل کاربر:** کل پروفایل (نام، آواتار، کیفیت، زیرنویس، صدا، PIN، اعلان‌ها) به‌صورت یک بلوب JSON با یک timestamp سینک می‌شود. اگر روی گوشی آواتار را عوض کنید و روی دسکتاپ همان لحظه کیفیت پیش‌فرض را تغییر دهید، هرکدام دیرتر سینک شود **کل تغییرات دیگری را از بین می‌برد**. کاربر: «چرا تنظیم کیفیتم برگشت؟»
- **راه‌حل:** merge فیلد‌به‌فیلد با timestamp per-field، یا حداقل جداکردن فیلدهای پرتغییر (پخش) از فیلدهای هویتی.

### `[DATA-10]` تنظیمات پلیر هر فیلم (زوم، تأخیر زیرنویس، سرعت) فقط محلی است و پاک‌شدنی
- **کجا:** `src/lib/player-prefs.ts:11-13,135-146`
- **مشکل کاربر:** موقعیت/تأخیر زیرنویس، حالت زوم هر عنوان، سرعت پخش و انتخاب موتور همه فقط در localStorage‌اند. با پاک‌کردن داده مرورگر/اپ یا نصب مجدد از بین می‌روند و روی دستگاه دوم اصلاً وجود ندارند. سناریو: زیرنویس فیلمی را با دقت روی +۲.۵ ثانیه تنظیم کرده‌اید؛ اپ را دوباره نصب می‌کنید و همه‌چیز صفر می‌شود.
- **راه‌حل:** انتقال این کلیدها به جدول پروفایل/per-title و افزودن به snapshot سینک.

### `[DATA-11]` تأخیر زیرنویس سراسری است، نه برای هر فیلم — به همه فیلم‌ها سرایت می‌کند
- **کجا:** `src/lib/player-prefs.ts:36-37,109-123`
- **مشکل کاربر:** کلید `nama-pref-sub-delay` واحد است. برای فیلمی که زیرنویسش ناهماهنگ است +۳ ثانیه تنظیم می‌کنید؛ فیلم بعدی هم با همان تأخیر پخش می‌شود و زیرنویس درستش خراب می‌شود.
- **راه‌حل:** کلید per-title مثل کاری که برای زوم انجام شده.

### `[DATA-12]` تماشای آفلاین در تاریخچه ثبت نمی‌شود تا آنلاین شوید
- **کجا:** `src/lib/mobile-downloads.ts` + `src/lib/mobile/userdata.ts:409-422`
- **مشکل کاربر:** فایل دانلود‌شده در جدول `dlitems` است ولی پیوندی به `progress` ندارد. اگر کاملاً آفلاین یک فیلم دانلودشده را ببینید، پیشرفت در Dexie نوشته می‌شود اما فقط وقتی به ابر می‌رود که بتواند slug را resolve کند؛ آفلاین این skip می‌شود و صف retry ندارد. با بستن اپ، پیشرفت تماشای آفلاین هرگز سینک نمی‌شود.
- **راه‌حل:** صف مقاوم برای پیشرفت‌های resolve-نشده + flush هنگام آنلاین‌شدن.

### `[DATA-13]` دانلودهای آفلاین بین حساب‌های یک دستگاه نشت می‌کنند
- **کجا:** `src/lib/mobile-downloads.ts:17-35` + `src/lib/mobile/db.ts:119-121`
- **مشکل کاربر:** برخلاف علاقه‌مندی/لیست/پیشرفت که با `userKey` جدا می‌شوند، جدول دانلودها هیچ فیلد حسابی ندارد و سراسری است. اگر حساب دیگری روی همان گوشی وارد شود، لیست دانلودهای شما (عناوین و پوسترها) را می‌بیند.
- **راه‌حل:** افزودن `userKey` به رکورد دانلود و فیلتر در لیست دانلودها.

### `[DATA-14]` جداسازی حساب موبایل با هش ضعیف و بدون تأیید مالکیت
- **کجا:** `src/lib/mobile/userdata.ts:60-64,85-110`
- **مشکل کاربر:** سمت موبایل فضای داده هر حساب را با هش ساده djb2 روی ۳۲ بیت می‌سازد (برخلاف سمت سرور که sha256 است). فضای کلید کوچک است و احتمال برخورد دو حساب وجود دارد → دیدن کتابخانه/تاریخچه حساب دیگر. همچنین نگاشت حساب‌ها فقط در localStorage است؛ با پاک‌شدن داده مرورگر، همه حساب‌ها به فضاهای خالی جدید می‌افتند و داده قبلی نامرئی می‌شود.
- **راه‌حل:** هم‌ترازی با سمت سرور (sha256 + تأیید توکن) و ذخیره نگاشت در Dexie نه فقط localStorage.

### `[DATA-15]` ورود آفلاین با حساب دوم می‌تواند داده حساب اول را نشان دهد
- **کجا:** `src/lib/identity.ts:31-37` + `src/app/api/identity/route.ts:126-128`
- **مشکل کاربر:** جابه‌جایی فضای per-account فقط با توکن معتبر و اتصال به سرور کار می‌کند. اگر آفلاین با حساب B وارد شوید در حالی که فضای حساب A فعال است، B داده‌های A را می‌بیند تا اولین اتصال موفق. روی دسکتاپ که اغلب VPN لازم است این حالت شایع است.
- **راه‌حل:** تا وقتی مالکیت تأیید نشده، به فضای «مهمان خالی» برو نه فضای حساب قبلی.

### `[DATA-16]` تغییر نام کالکشن باعث کالکشن تکراری در دستگاه دیگر می‌شود
- **کجا:** `src/lib/cloud.ts:901-912` + `src/app/api/cloud/merge/route.ts:150-153`
- **مشکل کاربر:** کالکشن‌ها با «نام» کلید می‌خورند نه یک id پایدار. اگر روی موبایل نام «فیلم‌های شب» را به «شب‌گردی» تغییر دهید، دسکتاپ که هنوز کالکشن قدیمی را دارد، نام جدید را «کالکشن تازه» می‌بیند و یک نسخه دوم می‌سازد. کاربر دو کالکشن یکسان می‌بیند.
- **راه‌حل:** کلید پایدار (uuid) برای کالکشن به‌جای نام.

## بخش رابط کاربری — تجربه ناقص

### `[UI-4]` نبود تاریخچه و پیشنهاد جستجوهای اخیر
- **کجا:** `src/app/search/page.tsx:91-102` + پنل جستجوی `Navbar.tsx`
- **مشکل کاربر:** پنل جستجوی نوار بالا وقتی خالی است هیچ‌چیز نشان نمی‌دهد و صفحه جستجو فقط لیست ثابت «جستجوهای پرتکرار» دارد. جستجوهای قبلی خود کاربر ذخیره/پیشنهاد نمی‌شوند؛ باید هر بار دستی تایپ کنید (روی موبایل با کیبورد فارسی دردسرساز است).
- **راه‌حل:** ذخیره محلی جستجوهای اخیر و نمایش‌شان در پنل خالی.

### `[UI-5]` کاروسل صفحه اول روی موبایل هرگز متوقف نمی‌شود
- **کجا:** `src/components/Hero.tsx:20-24,33-34`
- **مشکل کاربر:** توقف اسلاید فقط با ماوس (`onMouseEnter/Leave`) کار می‌کند — یعنی فقط دسکتاپ. روی موبایل هر ۷ ثانیه محتوا عوض می‌شود؛ اگر وسط خواندن توضیحات فیلم باشید، پرت می‌شوید. همچنین به تنظیم «کاهش حرکت» (reduce-motion) گوش نمی‌دهد.
- **راه‌حل:** توقف روی لمس/focus، دکمه توقف/ادامه، و غیرفعال‌کردن چرخش خودکار وقتی reduce-motion فعال است.

### `[UI-6]` هشدار «تغییرات ذخیره‌نشده» فقط هنگام بستن مرورگر کار می‌کند
- **کجا:** `src/components/library/SettingsForm.tsx:160-167`
- **مشکل کاربر:** فقط `beforeunload` (بستن/رفرش) هندل شده. اگر داخل اپ روی لینکی کلیک کنید، تغییرات تنظیمات بی‌هیچ هشداری از بین می‌رود — با اینکه نوار «تغییرات ذخیره‌نشده دارید» را دیده بودید.
- **راه‌حل:** رهگیری ناوبری داخلی (router events) و نمایش تأیید «خروج بدون ذخیره؟».

### `[UI-7]` اکشن‌های گروهی «تغییر وضعیت» و «افزودن به علاقه‌مندی» undo ندارند
- **کجا:** `src/components/library/MyListManager.tsx:201-225`
- **مشکل کاربر:** فقط حذف گروهی دکمه «بازگردانی» در Toast دارد؛ تغییر وضعیت انبوه و افزودن انبوه به علاقه‌مندی‌ها برگشت‌پذیر نیستند، در حالی که روی ده‌ها آیتم اعمال می‌شوند.
- **راه‌حل:** افزودن اکشن «واگرد» به همان Toastها.

---

# 🟡 فاز ۳ — پولیش (دسترس‌پذیری و کیفیت)

این‌ها اپ را از «خوب» به «حرفه‌ای» می‌رسانند. مخصوصاً دسترس‌پذیری (a11y) که برای کاربران کیبورد و صفحه‌خوان مهم است.

## دسترس‌پذیری (a11y)

### `[A11Y-1]` مودال‌ها focus-trap و بازگرداندن focus ندارند
- **کجا:** `CommandPalette.tsx:189-191`، `WelcomeAuth.tsx:105-116`، `SettingsForm.tsx:621`
- **مشکل کاربر:** با اینکه بعضی مودال‌ها `role="dialog"` دارند، کلید Tab به عناصر پشت مودال فرار می‌کند و بعد از بستن، focus به دکمه فراخوان برنمی‌گردد. برای کاربر کیبورد و صفحه‌خوان گیج‌کننده است. مودال خوش‌آمد هم focus را روی دکمه اصلی نمی‌گذارد.
- **راه‌حل:** focus-trap (چرخش Tab داخل مودال) + focus اولیه روی دکمه اصلی + بازگرداندن focus هنگام بستن.

### `[A11Y-2]` دیالوگ‌های «پاک کردن کل لیست» و «یادداشت» اصلاً نقش ARIA ندارند
- **کجا:** `MyListManager.tsx:508,635`
- **مشکل کاربر:** هر دو فقط `<div>` ساده هستند — نه `role="dialog"`، نه `aria-modal`، نه نام. صفحه‌خوان آن‌ها را به‌عنوان مودال معرفی نمی‌کند و focus مدیریت نمی‌شود.
- **راه‌حل:** افزودن `role="dialog" aria-modal="true" aria-labelledby` + focus-trap.

### `[A11Y-3]` مودال «منطقه خطر» به عنوانش وصل نیست
- **کجا:** `SettingsForm.tsx:621`
- **مشکل کاربر:** `role="alertdialog"` دارد اما `aria-labelledby` به عنوان («مطمئن هستید؟») وصل نشده و focus قفل نمی‌شود.
- **راه‌حل:** افزودن `aria-labelledby`/`aria-describedby` + focus اولیه روی «انصراف».

### `[A11Y-4]` input جستجو الگوی ARIA combobox کامل ندارد
- **کجا:** `Navbar.tsx:304-328`
- **مشکل کاربر:** پنل نتایج `role="listbox"` دارد اما خود input نه `role="combobox"`، نه `aria-expanded`، نه `aria-activedescendant`. صفحه‌خوان رابطه input و لیست نتایج را اعلام نمی‌کند.
- **راه‌حل:** افزودن ویژگی‌های combobox و `aria-activedescendant` همگام با آیتم فعال.

### `[A11Y-5]` هدف‌های لمسی کوچک‌تر از حد استاندارد
- **کجا:** `Hero.tsx:120-128` (نقطه‌های کاروسل ۶px)، `SettingsForm.tsx:89` (سوییچ ۲۴px)، `MyListManager.tsx:545-552` (دکمه‌ها ۳۲px)
- **مشکل کاربر:** چند هدف لمسی کوچک‌تر از حداقل توصیه‌شده ۴۴px هستند؛ روی موبایل زدن دقیق سخت است. نقطه‌های ناوبری کاروسل تقریباً غیرقابل‌لمس‌اند.
- **راه‌حل:** بزرگ‌کردن ناحیه لمسی به حداقل ۴۴px (با padding نامرئی).

## کیفیت و پولیش

### `[QOL-1]` حالت بارگذاری جستجو فقط یک نوار بی‌متن است
- **کجا:** `src/app/search/page.tsx:39-47`
- **مشکل کاربر:** هنگام واکشی فقط یک مستطیل پالس‌دار نمایش داده می‌شود بدون هیچ متنی. کاربر نمی‌داند نتایج در راه است یا صفحه گیر کرده.
- **راه‌حل:** متن وضعیت «در حال جستجو…» یا اسکلت شبکه کارت‌ها.

### `[QOL-2]` فرم جستجوی صفحه اجازه ثبت خالی می‌دهد
- **کجا:** `src/app/search/page.tsx:70-74`
- **مشکل کاربر:** برخلاف نوار بالا (که گارد `if (!q.trim()) return` دارد)، اینجا ثبت خالی به `/search?q=` می‌رود و صفحه نتایج تهی نشان می‌دهد.
- **راه‌حل:** افزودن گارد trim قبل از ناوبری.

### `[QOL-3]` فضاهای مهمان یتیم پس از خروج انباشته می‌شوند
- **کجا:** `src/lib/mobile/userdata.ts:90-95` + `src/app/api/identity/route.ts:105-113`
- **مشکل کاربر:** بعد از هر خروج یک فضای «مهمان» تصادفی جدید ساخته می‌شود و فضای قبلی هرگز پاک نمی‌شود؛ فقط فضای دیسک را اشغال می‌کند و اگر مهمان چیزی ذخیره کرده باشد، دیگر قابل‌دسترس نیست.
- **راه‌حل:** جمع‌آوری زباله (GC) فضاهای مهمان بی‌ارجاع، یا یک فضای مهمان ثابت.

### `[QOL-4]` مرتب‌سازی تاریخچه با مقایسه رشته‌ای انجام می‌شود
- **کجا:** `src/lib/mobile/userdata.ts:533,559,587`
- **مشکل کاربر:** ترتیب «اخیراً دیده‌شده» با مقایسه رشته‌ای تاریخ است. تا وقتی همه ISO باشند درست است، اما اختلاط فرمت‌های تاریخ (که از merge ابری می‌آید) می‌تواند ترتیب اشتباه بدهد.
- **راه‌حل:** مقایسه عددی با `Date.parse` و نرمال‌سازی همه نوشته‌ها به ISO.

---

# 💡 قابلیت‌های پیشنهادی — «چیزهایی که بهتر است باشد»

> **نکته مثبت مهم:** فریم محصول بسیار بالغی است. پلیر موبایل (۳۲۰۰+ خط) در سطح جهانی است: ژست روشنایی/صدا، تایمر خواب، زوم، تأخیر زیرنویس، MediaSession، مینی‌پلیر، wake-lock، data-saver و watch-party («سینما») همه موجودند. پس پیشنهادها روی **شکاف‌های استراتژیک** تمرکز دارند، نه پایه‌ای‌ها.

## سه شکاف بزرگ که فریم را از رقبا (نتفلیکس/فیلیمو/نماوا) عقب می‌اندازد

| شکاف | چرا مهم است | وضعیت |
|---|---|---|
| **۱. ارسال به تلویزیون (Chromecast / AirPlay / DLNA)** | قلب تجربه «سینمای خانگی» است — تماشا روی تلویزیون بزرگ. رقبا این را دارند. هیچ اثری در کد نیست. | از صفر |
| **۲. توصیه شخصی‌سازی‌شده واقعی («چون X را دیدی»)** | موتور اصلی کشف محتوا. الان «پیشنهاد برای شما» فقط ترندینگ است. | داده کامل موجود، فقط الگوریتم لازم |
| **۳. چند پروفایل در یک حساب (پروفایل خانوادگی)** | استاندارد صنعت. الان فقط یک پروفایل به‌ازای حساب. | زیرساخت جداسازی داده آماده |

## فاز ۱ محصولی — بیشترین اثر، زیرساخت نسبتاً آماده

### `[FEAT-1]` موتور توصیه شخصی («چون X را دیدی») در صفحه خانه
داده رفتاری (تاریخچه، امتیاز، علاقه‌مندی، ژانرهای پرتکرار) کامل موجود است و تابع `getSimilar()` هم هست؛ فقط باید به یک ردیف در صفحه خانه تبدیل شود. **بیشترین بازگشت سرمایه.**

### `[FEAT-2]` انتخاب چند تراک صوتی (دوبله/زبان اصلی) داخل پلیر
فایل‌های MKV اغلب چند تراک صوتی دارند و موتور (`audio-guard.ts` + ExoPlayer) آن‌ها را می‌شناسد؛ فقط UI انتخاب تراک کم است. کاربر باید بتواند بین دوبله فارسی و صوت اصلی سوییچ کند بدون عوض‌کردن «نسخه».

### `[FEAT-3]` چند پروفایل خانوادگی در یک حساب
جداسازی تاریخچه و توصیه اعضای خانواده — استاندارد صنعت. زیرساخت جداسازی داده (`wipeCloudAccountData`, isolation) موجود است؛ باید به لایه پروفایل تبدیل شود.

### `[FEAT-4]` انتخاب کیفیت و مدیریت فضای دانلود
- انتخاب کیفیت هنگام دانلود (الان کیفیت فعلی را می‌گیرد بدون انتخاب کاربر) — لیست کیفیت‌ها موجود است.
- نمای تجمیعی فضای مصرفی/آزاد + پاک‌سازی گروهی (الان فقط حذف تک‌به‌تک) — تابع `fileStat` موجود است.

## فاز ۲ محصولی — تمایزساز بزرگ (زیرساخت از صفر)

### `[FEAT-5]` اعلان Push واقعی
الان اعلان‌ها فقط درون‌برنامه‌ای و لحظه‌ای ساخته می‌شوند؛ کاربر خارج از اپ از قسمت جدید سریال یا یادآوری ادامه تماشا خبردار نمی‌شود. نیاز به FCM (اندروید) / Notification API (دسکتاپ).

### `[FEAT-6]` دانلود خودکار قسمت بعدی سریال
تجربه بی‌وقفه سریال آفلاین (فیچر شناخته‌شده نتفلیکس). نیاز به منطق صف بر اساس اپیزود بعدی.

### `[FEAT-7]` دکمه «رد کردن تیتراژ/خلاصه قبل»
تنظیم `skipIntro` در پروفایل هست ولی بازه تیتراژ در داده تعریف نشده و دکمه پیاده نشده. نیاز به فیلد `introStart/introEnd` روی اپیزود.

## فاز ۳ محصولی — تکمیل تجربه (پورت آسان)

- **`[FEAT-8]` تأخیر زیرنویس و تایمر خواب در پلیر دسکتاپ** — در موبایل هست، فقط پورت لازم است.
- **`[FEAT-9]` تاریخچه جستجو + autocomplete هنگام تایپ** — تابع جستجو آماده است.
- **`[FEAT-10]` مرتب‌سازی/فیلتر داخل لیست تماشا و علاقه‌مندی‌ها** — با رشد لیست، پیداکردن سخت می‌شود.
- **`[FEAT-11]` صفحه «مرور سال / آمار تماشا»** — تابع `getUserStats` (دقایق، ژانرها) موجود است؛ هم‌افزا با «تاریک‌خانه» برای اشتراک‌گذاری.
- **`[FEAT-12]` درون‌ریزی (Import) پشتیبان JSON** — الان فقط خروجی (export) هست، بازگردانی نیست.
- **`[FEAT-13]` فیلتر پیشرفته** — چند ژانر هم‌زمان، بازه سال، مدت‌زمان، دوبله/زیرنویس.
- **`[FEAT-14]` راهنمای گام‌به‌گام اولیه (Onboarding)** — آموزش قابلیت‌های خاص (سینما، تاریک‌خانه، مینی‌پلیر) به کاربر جدید.
- **`[FEAT-15]` نظر با هویت حساب** — الان نام نویسنده نظر **دستی تایپ** می‌شود (جعل‌پذیر)؛ باید به هویت حساب گره بخورد.

## نقطه قوت قابل بهره‌برداری

قابلیت **watch-party («سینما»)** یک تمایز واقعی است که حتی خیلی از رقبای بزرگ ندارند. با افزودن **لینک دعوت دوستان به اتاق** (`cinema.ts` زیرساختش را دارد) می‌تواند به یک موتور رشد ویروسی تبدیل شود.

---

# 📊 جمع‌بندی و اولویت پیشنهادی

## اگر فقط ۵ کار وقت داشتید، این‌ها را انجام دهید

1. **`[DATA-2]` + `[DATA-1]`** — مدل سینک را از «فقط union» به tombstone تغییر دهید. این ریشه بیشتر شکایت‌های «داده‌ام برگشت / سینک نشد» است.
2. **`[UI-1]`** — دکمه‌های اکشن نمای شبکه‌ای را روی موبایل قابل‌دسترس کنید (الان عملاً وجود ندارند).
3. **`[DATA-5]` + `[DATA-7]`** — LWW واقعی برای امتیاز/وضعیت و پیشرفت per-episode.
4. **`[UI-2]` + `[UI-3]`** — نمایش خطای شبکه در جستجو، و تأیید قبل از لغو دانلود.
5. **`[FEAT-1]`** — ردیف توصیه شخصی در خانه (داده آماده است، اثر زیاد).

## آمار یافته‌ها

| حوزه | فاز ۱ (بحرانی) | فاز ۲ (مهم) | فاز ۳ (پولیش) | جمع |
|---|:---:|:---:|:---:|:---:|
| ذخیره داده | ۴ | ۱۲ | ۲ | ۱۸ |
| رابط کاربری | ۳ | ۴ | ۹ | ۱۶ |
| قابلیت‌ها | — | ۷ | ۸ | ۱۵ |
| **جمع** | **۷** | **۲۳** | **۱۹** | **۴۹** |

> **یادداشت:** این گزارش مکمل `AUDIT.md` موجود در ریپو است (که ممیزی فنی/امنیتی بود). موارد این گزارش عمدتاً روی **تجربه کاربر و پایداری داده** تمرکز دارند که در آن ممیزی کم‌رنگ بودند — به‌ویژه کل بخش سینک/تعارض/رستاخیز داده.

<sub>تهیه‌شده با بررسی مستقیم کد نسخه ۰.۲۶.۰ · شماره خط‌ها مربوط به همان نسخه است.</sub>

</div>

